<% content_for :title do %><%= params[:id] %><% end %>

<div id='main-container' class='container'>
  <%= render :partial=>'/flash_msg', layout: 'shared/flash_messages' %>
  
  <h1>Dashboard</h1>
  <h2><%= params[:id] %></h2>

  <h3>Current Ingest</h3>
  <div>QA Tasks</div>
  <ul>
    <li>
      <%= link_to 'Records', krikri.records_path(provider_id: params[:id]) %>
    </li>
    <li>
      <%= link_to 'Reports', krikri.reports_path(provider_id: params[:id]) %>
    </li>
  </ul>
</div>
