<% content_for :title do %>New Harvest Source<% end %>

<h1>New Harvest Source</h1>

<%= render partial: 'form', locals: { action: 'create' } %>

<%= link_to 'Back',
    institution_harvest_sources_path(
      institution_id: @harvest_source.institution_id
    )
%>
